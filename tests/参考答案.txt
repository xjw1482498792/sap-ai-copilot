-------------------------------------------------------------------------------------------------------------------------------------------------
自我介绍：
	面试官您好，我叫肖家伟，本科毕业于大连民族大学软件工程专业。目前在松下信息系统担任SAP系统开发工程师，有两年软件开发经验。
	过去两年，我主要参与松下工厂的采购、生产、销售和财务等模块的报表和跨系统接口开发。熟悉软件开发流程，从需求分析到开发测试再到上线和运维。
	最近独立完成了一个ai agent项目，SAP智能查询助手。他的主要功能是把用户的自然语言转为SQL，查询并返回业务数据。通过RAG和tool工具调用，提高SQL生成准确率的同时，降低token消耗。通过langgraph自修复流程，提高SQL可执行率。
	我认为的优势是，既有企业开发实践经验，又能通过python把需求转化为实际应用。最后希望能够加入到华为有关团队中。以上谢谢。

-------------------------------------------------------------------------------------------------------------------------------------------------

1）项目整体架构、你的分工，哪些代码是你独立开发？
执行流程：
	当用户输入自然语言后，首先本地的BGE模型会将自然语言转换为512维度向量，然后chroma向量库根据向量余弦相似度，检索出前5条完整SAP业务表结构。接着进入langgraph流程中，共有三个节点：generate节点负责生成SQL；execute节点负责执行SQL；reflect节点负责反馈错误。
	进入generate节点后，将RAG检索召回的5个完整的表结构用于生成SQL，全部表的简略结构用于调用tool，均放入system提示词，将用户问题放入user提示词，按照规范将tool定义的方法名和参数一并传入API调用接口，由模型给出回答。模型回答通常有两种：1是直接生成SQL，进入execute节点  2是RAG召回的表数量不全，需要调用tool查询其他表的结构。   根据模型返回的表名，由程序调用方法查询出完整表结构。   将模型调用tool的请求和方法返回的结果维护至历史会话中，开始下一轮API调用，直至表结构足以生成SQL或者调用次数达到上限。
	进入execute节点后，调用SQLite接口，真正地执行SQL。执行成功的标准是是否抛出异常，将执行SQL和结果计入State状态对象中。 执行成功，流程结束，执行失败且在自修复次数内会进入reflect反馈节点。
	在reflect节点中，从state状态对象取出历史执行记录，分析最近的重复性错误。如果最近发生了2次或以上同类型的错误，记录重复的错误和发生的次数。 当下一轮进入generate节点后，会把历史执行记录和最近错误一并放入user提示词，开始下一轮的生成。


技术栈：
	主要以python语言开发为主，langgrapth框架维护流程和状态，BGE模型做中文向量化，Chroma向量库做向量检索，Deepseek API分析和生成SQL，SQLite数据库存储真实的业务数据。

架构层级：
	主要分为四大层级：
	1是app交互层，负责接收用户问题，返回答案
	2是ai流程层，封装langgraph三个节点和对应的边
	3是ai能力层，封装api调用，tool定义，prompts整理和rag召回等方法
	4是db持久层，封装SQLite连接

核心模块：
	按照功能主要分为rag，tool，generate，execute，reflect，config，log等模块
     
我的职责：	 
	从技术选型到流程设计和api调用核心代码均有我独立完成，期间借助AI做可行性检查和优化，比如选取轻量级的BGE模型做向量转化，自修复次数达到上线的边界逻辑检查，截取SQL的正则表达式等

我的职责及最核心的内容：
	主要是定义langgraph三个节点和对应的边。最重要的节点generate。包括封装提示词，tool的定义和调用，api调用，维护历史会话


-------------------------------------------------------------------------------------------------------------------------------------------------

bug与难点：
	项目最大的难点不是传统的python语法，而是如何处理大模型输出的不确定性。
	虽然我在system prompt中明确要求模型只返回纯SQL，并把temperature设置为0，但模型仍然可能返回Markdown围栏、中文开场白，和后续解释话语等。
	对于模型输出的概率性无法避免，我在python中做了多层兜底机制。
	首先通过自定义方法对模型输出结果进行标准化：去掉Markdown围栏，定位第一个SELECT、WITH等关键字，截掉SQL后面的解释性内容。
	这里曾遇到一个bug，最初的规则是把所有以中文开头的行都算中文解释，但SQL中可能存在中文字段别名，导致合法SQL被误截。
	后来我把规则改为白名单匹配，只有中文行中包含“所以”，“根据”等明显话术时，才认为是解释性内容。
	清理完成后，系统会把SQL交给SQLite实际执行。执行成功才返回结果；如果出现字段不存在、语法错误等其他错误，还会把执行结果计入langgraph状态，经过reflect反馈节点后重新生成。
	这样就形成了“提示词约束-python清洗-数据库验证-agent自修复”多层容错机制

优化：
2、某些问题模型理解错误，如今年的大客户有哪些
	模型对于大客户定义不清楚，导致即便生成SQL顺序执行，但是与实际结果有偏差。
	后期解决方案，当遇到歧义的时候，模型反馈出几种理解由客户再一次选择确定。
-------------------------------------------------------------------------------------------------------------------------------------------------

为什么选择华为OD，不选互联网、其他外包？
为什么从上家公司离职？
答：1华为业务体量够大，能接触工业级大型项目，已经足够我学习成长；
	2OD能长期稳定在华为园区办公，周围都是优秀同事，愿意长期深耕；
	3最重要上家公司技术栈单一，且开发受限于SAP客户端内，向往使用python等主流开发语言
-------------------------------------------------------------------------------------------------------------------------------------------------	
3）能接受长期加班、版本上线熬夜、紧急需求插队吗？
我有过项目开发经验，知道版本上线的重要性，
日常的话尽量提高效率减少被动加班，
项目周期不可避免的加班是正常的，可以接受
-------------------------------------------------------------------------------------------------------------------------------------------------	
第一年：熟悉内部开发工具和流程，尽快融入团队，逐步具备独立开发任务的能力 
第二年：系统提升python相关技术能力，包括并发编程、性能优化和问题排查等，能独立负责重要开发任务
第三年：经过2年和沉淀和技术积累，希望能够成长为团队可靠的核心成员，能够分析复杂问题和设计技术方案。
-------------------------------------------------------------------------------------------------------------------------------------------------	
优点是喜欢追求新技术，持续关注模型发展动态，学习能力强，执行力强；
缺点是遇到问题容易自我钻研过深而缺少一些协作交流，工作后逐步认识到，不同类型的问题会有不同职责的人对应，分工明确才能提高效率
-------------------------------------------------------------------------------------------------------------------------------------------------	
2）和同事需求冲突、排期矛盾怎么处理？
在我以往的工作经历中也会经常遇到同时对应多个开发需求的情况，我一般了解对方的UAT期限或者或者上线时间等优先级，如果实在无法协调会跟负责人申请额外开发资源。