# Python 后端面试模拟题及参考答案

> 使用方法：先只看题目作答，再对照“参考答案”。答案按面试口述长度编写，实际回答时应结合自己的项目经历补充。

## 一、Python 基础

### 1. Python 的垃圾回收机制是什么？循环引用如何处理？

**参考答案：** CPython 主要使用引用计数：对象引用数归零时立即回收。引用计数无法单独解决循环引用，因此 CPython 还提供分代循环垃圾回收器，把对象分为不同代，定期扫描容器对象形成的引用环，并回收不可达对象。`gc` 模块可以查看、启停或手动触发循环回收。带有资源的对象不应依赖垃圾回收，应使用 `with` 或显式关闭。

### 2. 什么是 GIL？为什么 Python 多线程不能充分利用多核？

**参考答案：** GIL 是 CPython 的全局解释器锁，同一进程中同一时刻通常只有一个线程执行 Python 字节码。因此 CPU 密集型任务使用多个 Python 线程很难获得多核加速，但 I/O 等待时线程会释放 GIL，所以多线程仍适合 I/O 密集型任务。CPU 密集型任务通常使用多进程、原生扩展或可释放 GIL 的数值计算库。GIL 是 CPython 的实现细节，不等于 Python 语言本身的规定。

### 3. `is` 和 `==` 有什么区别？

**参考答案：** `is` 比较两个引用是否指向同一个对象，`==` 调用对象的相等性逻辑比较值。判断 `None` 应写 `x is None`。小整数和部分字符串可能因驻留机制而指向同一对象，但这是实现优化，不能依赖它判断值相等。

### 4. 浅拷贝、深拷贝和赋值引用有什么区别？

**参考答案：** 赋值只增加对同一对象的引用；浅拷贝创建新的外层容器，但内部元素仍与原对象共享；深拷贝递归复制内部对象。可分别用赋值、`copy.copy()` 和 `copy.deepcopy()`。深拷贝成本高，也不一定符合业务语义，实际开发中应先明确哪些层需要隔离。

### 5. `finally` 一定会执行吗？

**参考答案：** 正常退出 `try`、发生异常、执行 `return` 或 `break` 时，`finally` 通常都会执行，并且发生在函数真正返回前。但进程被强制终止、解释器崩溃、机器断电或调用 `os._exit()` 时不能保证执行。不要在 `finally` 中写 `return`，否则可能覆盖原返回值或异常。

## 二、Python 进阶

### 6. 什么是闭包？常见陷阱是什么？

**参考答案：** 闭包是函数与其定义环境中自由变量的组合。它可用于保存状态、实现函数工厂和装饰器。常见陷阱是循环变量的晚绑定：内部函数调用时才读取变量，导致多个函数得到相同的最终值。可通过默认参数提前绑定，例如 `lambda x=i: x`。闭包状态较隐蔽，复杂场景可改用类。

### 7. 手写一个保留原函数信息的带参数装饰器。

**参考答案：**

```python
from functools import wraps

def retry(times=3):
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            last_error = None
            for _ in range(times):
                try:
                    return func(*args, **kwargs)
                except Exception as exc:
                    last_error = exc
            raise last_error
        return wrapper
    return decorator
```
```python
def decorator_function(original_function):
    def wrapper(*args, **kwargs):
        # 调用前
        print("执行前")

        result = original_function(*args, **kwargs)

        # 调用后
        print("执行后")

        return result
    return wrapper

@decorator_function
def target_function():
    print("原函数执行")
```
`retry(times)` 返回装饰器，装饰器返回包装函数；`wraps` 用于保留原函数的名称、文档等元数据。生产环境还应限制可重试异常、增加退避策略，并确认操作具有幂等性。

### 8. 迭代器和生成器有什么区别？

**参考答案：** 迭代器实现 `__iter__()` 和 `__next__()`，通过 `next()` 逐个产出元素，耗尽时抛出 `StopIteration`。生成器是创建迭代器的一种简洁方式，函数中使用 `yield` 即可暂停并保存执行现场。生成器按需计算、内存占用低，适合流式数据和大数据集，但通常只能单向遍历一次。

### 9. `with` 的底层原理是什么？

**参考答案：** `with` 基于上下文管理协议。进入代码块时调用 `__enter__()`，离开时无论正常还是异常都会调用 `__exit__(exc_type, exc, traceback)`。若 `__exit__` 返回真值，异常会被抑制。它适合管理文件、锁、数据库连接和事务等必须释放的资源，也可用 `contextlib.contextmanager` 快速实现。

### 10. `__new__` 和 `__init__` 有什么区别？

**参考答案：** `__new__` 负责创建并返回实例，是静态创建阶段；`__init__` 在实例创建后初始化它，不应返回值。如果 `__new__` 返回的不是当前类实例，通常不会调用当前类的 `__init__`。不可变类型定制、单例和元类相关场景常会重写 `__new__`。

## 三、并发编程

### 11. 线程、进程和协程分别适合什么场景？

**参考答案：** 进程拥有独立地址空间，隔离性强、通信成本高，适合 CPU 密集型任务；线程共享进程内存，切换与通信成本较低，但需要同步，适合阻塞型 I/O；协程由事件循环在用户态调度，单线程即可承载大量并发 I/O，但要求调用链使用非阻塞 API。选择方案时要考虑任务性质、隔离要求和依赖是否支持异步。

### 12. 死锁产生的四个必要条件是什么？如何避免？

**参考答案：** 四个条件是互斥、占有且等待、不可剥夺、循环等待。破坏任一条件即可避免死锁。常见做法包括统一加锁顺序、缩小锁粒度和持锁时间、使用超时获取、避免嵌套锁，以及用队列传递数据。排查时可打印线程栈和锁等待信息。

### 13. `Lock`、`RLock`、`Semaphore` 和线程安全队列有什么区别？

**参考答案：** `Lock` 是普通互斥锁；`RLock` 允许同一线程重复获取，适合递归或嵌套调用；`Semaphore` 允许固定数量的线程同时访问资源，适合连接数、并发数限制；`queue.Queue` 内部已实现同步，适合生产者—消费者模型，通常比手动共享列表加锁更安全。

### 14. `async/await` 和事件循环是如何工作的？

**参考答案：** `async def` 创建协程函数，调用后得到协程对象。协程执行到 `await` 时，如果等待对象未完成，就主动让出控制权；事件循环继续执行其他就绪任务，并在 I/O 完成后恢复该协程。因此异步的优势来自等待期间复用线程，不是让 CPU 计算自动并行。协程中调用阻塞函数会卡住整个事件循环，应改用异步库或放入线程池。

## 四、面向对象

### 15. 封装、继承和多态分别是什么？

**参考答案：** 封装把状态和行为组织在对象内部，通过稳定接口隐藏实现；继承复用或扩展父类行为，但要避免层次过深；多态让不同对象通过相同接口表现出各自行为，例如多个对象都实现 `save()`。Python 更强调鸭子类型：重点是对象能做什么，而不一定要求继承同一个父类。

### 16. Python 多继承时如何确定方法查找顺序？

**参考答案：** Python 使用 MRO 和 C3 线性化算法，在保持局部继承顺序、单调性且每个类只出现一次的前提下生成查找顺序，可通过 `Class.__mro__` 查看。`super()` 并不是简单调用“父类”，而是沿当前实例所属类的 MRO 查找下一个实现。因此协作式多继承中的方法应使用兼容签名并继续调用 `super()`。

### 17. 类属性和实例属性有什么区别？

**参考答案：** 类属性保存在类对象上，通常被所有实例共享；实例属性一般保存在实例的 `__dict__` 中。读取 `obj.x` 时先找实例，再沿类的 MRO 查找。给 `obj.x` 赋值通常会创建或修改实例属性，并遮蔽同名类属性。可变类属性会被实例共享，若无意共享，应该在 `__init__` 中创建。

### 18. 写出两种单例实现方式，并说明风险。

**参考答案：** 可以重写 `__new__` 缓存唯一实例，也可以用模块级对象——Python 模块只加载一次，通常是最简单的单例。还可使用元类或装饰器。要注意线程安全、测试隔离、序列化和多进程问题；“单进程唯一”不代表分布式系统中唯一。许多场景用依赖注入显式管理对象生命周期更清晰。

## 五、MySQL

### 19. InnoDB 索引为什么使用 B+ 树？

**参考答案：** B+ 树分支多、树高低，可以减少磁盘 I/O；非叶子节点只保存键和指针，单页可容纳更多索引项；叶子节点有序并通过链表连接，适合范围查询。InnoDB 主键索引的叶子节点保存整行数据，二级索引叶子节点保存主键值，因此通过二级索引查其他列可能发生回表。

### 20. 什么是联合索引的最左前缀原则？哪些情况可能导致索引效果差？

**参考答案：** 对索引 `(a, b, c)`，查询通常要从最左列开始连续利用其有序性，例如 `a`、`a,b`、`a,b,c`。跳过 `a` 通常不能有效使用该索引进行定位。对索引列做函数或隐式类型转换、以 `%` 开头的模糊匹配、低选择性条件、范围条件后的列不能继续用于缩小扫描范围等，都可能使索引效果变差。最终要用 `EXPLAIN` 和实际数据分布判断，而不是只背规则。

### 21. 事务的 ACID 和隔离级别是什么？

**参考答案：** 原子性表示事务整体成功或回滚；一致性表示事务前后满足约束；隔离性表示并发事务相互隔离；持久性表示提交结果不会丢失。SQL 标准隔离级别从低到高是读未提交、读已提交、可重复读、串行化。InnoDB 默认通常是可重复读，并结合 MVCC 和锁处理一致性读及当前读。

### 22. 一条慢 SQL 应如何排查和优化？

**参考答案：** 先通过慢查询日志或监控确认 SQL、耗时、频率和扫描行数；再用 `EXPLAIN`/`EXPLAIN ANALYZE` 看访问类型、索引、估算与实际行数、排序和临时表；检查数据分布、索引设计、返回列数、关联与过滤条件；必要时重写 SQL 或调整表结构。最后必须在接近生产的数据量上验证，并关注写入成本，不能为了单条查询无限加索引。

### 23. 深分页为什么慢？如何优化？

**参考答案：** `LIMIT 1000000, 20` 需要先扫描或排序大量记录再丢弃前面的行，偏移越大越慢。可使用基于稳定排序键的游标分页，例如 `WHERE id > last_id ORDER BY id LIMIT 20`；必须跳页时可先通过覆盖索引查主键，再回表关联取完整数据。分页排序应稳定，常用时间与主键组成联合游标。

## 六、Redis

### 24. Redis 常见数据结构及场景有哪些？

**参考答案：** String 用于缓存、计数器和分布式锁值；Hash 用于对象字段；List 用于简单队列或时间线；Set 用于去重、交并集；Sorted Set 用于排行榜和带权重队列。实际还包括 Bitmap、HyperLogLog、Geo、Stream 等。选择结构时要同时考虑操作复杂度、内存和是否需要原子操作。

### 25. 缓存穿透、击穿和雪崩有什么区别？

**参考答案：** 穿透是反复查询不存在的数据，可缓存空值、参数校验或使用布隆过滤器；击穿是热点键失效瞬间大量请求打到数据库，可用互斥重建、逻辑过期和热点预热；雪崩是大量键同时失效或 Redis 故障，可让过期时间随机化、分批预热、限流降级并提高缓存集群可用性。无论哪种方案都要考虑数据库兜底容量。

### 26. RDB 和 AOF 有什么区别？

**参考答案：** RDB 周期性生成数据快照，文件紧凑、恢复快，但两次快照之间的数据可能丢失；AOF 记录写命令，数据丢失窗口更小，但文件通常更大，恢复和重写有额外成本。Redis 可同时启用两者。具体选择取决于可接受的数据丢失量、恢复时间目标和磁盘性能，缓存数据也不应被当作唯一事实来源。

### 27. Redis 分布式锁如何正确释放？

**参考答案：** 获取锁可使用 `SET key random_value NX PX ttl`，其中值必须是客户端唯一标识。释放时要通过 Lua 脚本原子地“比较值后删除”，防止误删已由其他客户端获得的锁。TTL 防止客户端宕机造成永久死锁，但任务超时会导致锁提前失效，可使用合理 TTL、续期机制和 fencing token。对强一致场景，应评估专门的协调系统或数据库约束。

## 七、计算机网络

### 28. TCP 为什么是三次握手、四次挥手？

**参考答案：** 三次握手使双方确认彼此的发送和接收能力，并同步初始序列号；只进行两次无法让服务端确认客户端已收到自己的确认。关闭时 TCP 是全双工的，两个方向需分别关闭；一方发送 FIN 后，对方可能仍有数据要发送，所以 ACK 和自身 FIN 通常分开，形成四次挥手。主动关闭方通常进入 `TIME_WAIT`，以处理延迟报文并确保最后的 ACK 可重传。

### 29. TCP 和 UDP 有什么区别？

**参考答案：** TCP 面向连接，提供可靠、有序的字节流，并具有重传、流量控制和拥塞控制；UDP 无连接，以数据报为单位，不保证到达和顺序，协议开销和时延较低。文件传输、数据库连接常用 TCP；实时音视频、DNS 和允许应用层自行控制可靠性的场景常用 UDP。HTTP/3 基于 QUIC，而 QUIC 构建在 UDP 之上并在用户态实现可靠传输。

### 30. HTTPS 的握手和加密流程是什么？

**参考答案：** 客户端发起 TLS 握手并发送支持的版本、算法和随机数；服务端返回所选参数与证书；客户端验证证书链、域名和有效期；双方通过密钥交换计算会话密钥，之后使用对称加密传输 HTTP 数据。非对称密码主要用于身份认证和密钥协商，对称加密用于高效传输。现代 TLS 1.3 对流程做了简化，并通常使用具备前向安全性的临时密钥交换。

### 31. 常见 HTTP 状态码有哪些？

**参考答案：** `200` 成功，`201` 已创建，`204` 成功但无响应体；`301/308` 永久重定向，`302/307` 临时重定向，`304` 缓存仍有效；`400` 请求错误，`401` 未认证，`403` 已识别但无权限，`404` 不存在，`409` 冲突，`429` 请求过多；`500` 服务端错误，`502` 网关收到无效响应，`503` 暂不可用，`504` 网关超时。

## 八、Linux 与工程化

### 32. 如何排查某服务端口被谁占用，并查看相关日志？

**参考答案：** 可用 `ss -lntp` 查看监听端口和进程，用 `ps -ef` 或 `ps aux` 查看进程，用 `top`/`htop` 观察资源；systemd 服务可用 `systemctl status 服务名` 和 `journalctl -u 服务名`；文本日志可用 `tail -f`、`grep`、`less`、`awk` 等筛选。排查时应先明确时间范围、请求标识和错误关键字，避免直接对超大日志做无范围扫描。

### 33. Python logging 的日志级别和最佳实践是什么？

**参考答案：** 常见级别从低到高是 `DEBUG`、`INFO`、`WARNING`、`ERROR`、`CRITICAL`。库代码应使用模块级 logger，而不是直接配置根 logger；生产日志应包含时间、级别、服务、请求或链路 ID 和异常栈。使用参数化日志，如 `logger.info("user=%s", user_id)`，避免无必要的字符串拼接；同时配置轮转、保留周期、脱敏和集中采集。

### 34. Python 服务性能优化的一般步骤是什么？

**参考答案：** 先定义指标并测量，使用监控、火焰图、`cProfile`、采样分析器或数据库追踪定位瓶颈，再针对性优化。常见手段包括改进算法与数据结构、减少 I/O 和数据库往返、批量处理、合理缓存、连接池、并发模型调整及减少序列化成本。优化后要基准测试并观察延迟分位数、吞吐、CPU、内存和错误率，避免凭感觉优化。

## 九、综合场景题

### 35. 接口流量突然增长十倍，你会如何保护系统？

**参考答案：** 先确认流量来源和核心瓶颈，再按入口、应用、缓存、数据库分层处理：入口限流和鉴权；应用侧超时、熔断、降级、隔离线程池；热点数据缓存并防击穿；非实时任务进入消息队列削峰；数据库减少慢查询并保护连接数。扩容只能缓解容量问题，仍需确保下游不会被放大流量压垮，同时要准备核心指标、告警和应急开关。

### 36. 如何设计一个高并发库存扣减接口，防止超卖？

**参考答案：** 数据库可使用条件更新：`UPDATE stock SET amount = amount - 1 WHERE sku_id = ? AND amount > 0`，根据受影响行数判断是否成功，避免“先查再改”的竞争。高流量场景可在 Redis 中原子预扣并通过消息队列异步落库，但必须处理消息幂等、重复消费、失败补偿、缓存与数据库一致性。订单请求应携带幂等键，最终以可审计的库存流水和数据库约束兜底。

### 37. 线上 Python 进程内存持续上涨，如何排查？

**参考答案：** 先区分是真正泄漏、缓存增长、流量增长还是内存碎片，观察 RSS、对象数量和 GC 指标。用 `tracemalloc` 对比内存快照，结合对象引用分析定位增长类型；检查无界缓存、全局容器、未关闭连接、任务积压、循环引用以及 C 扩展。修复后用稳定流量压测验证；临时重启或限制进程生命周期只能作为止血措施。

### 38. 数据库更新成功但缓存删除失败，如何保证最终一致？

**参考答案：** 常见策略是先更新数据库再删除缓存，并通过重试机制保证删除最终成功；可将待重试事件写入可靠消息或基于 binlog 订阅异步失效缓存。读取时设置合理 TTL 作为兜底。严格来说短时间不一致仍可能存在，因此要结合业务容忍度设计；强一致业务应减少缓存参与关键判定，使用数据库事务、版本号或其他一致性机制。

## 十、面试官常见连续追问

1. 你刚才给出的方案，在多进程、多实例部署下还成立吗？
2. 如果请求执行到一半超时，重试会不会产生重复数据？
3. 这个索引提升了查询性能，会对写入和存储造成什么影响？
4. 缓存不可用时，数据库能否承受全部流量？
5. 你如何用数据证明优化有效，而不是偶然波动？
6. 方案发生最坏情况时，如何降级、回滚和审计？

回答这类追问时，可使用统一结构：**结论 → 原理 → 适用边界 → 项目实例 → 验证指标**。
